const fs = require('fs'); const path = require('path'); const dir = 'client/src/pages'; fs.readdirSync(dir).filter(f => f.endsWith('.jsx')).forEach(f => { let p = path.join(dir, f); let c = fs.readFileSync(p, 'utf8'); let nc = c.replace(/\s+itemScope(?=\s|>)/g, '').replace(/\s+itemType="[^"]+"/g, '').replace(/\s+itemProp="[^"]+"/g, ''); if (c !== nc) { fs.writeFileSync(p, nc, 'utf8'); console.log('Fixed ' + f); } });
