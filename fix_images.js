const fs = require("fs"); const path = require("path"); function w(d) { fs.readdirSync(d).forEach(f => { let p = path.join(d, f); if (fs.statSync(p).isDirectory()) w(p); else if (p.endsWith(".jsx")) { let c = fs.readFileSync(p, "utf8"); let nc = c.replace(/<img([^>]*)loading="lazy"([^>]*)>/g, (m, p1, p2) => m.includes("width=") ? m : "<img" + p1 + "loading=\"lazy\" width=\"600\" height=\"400\"" + p2 + ">"); if (c !== nc) { fs.writeFileSync(p, nc); console.log("Fixed " + p); } } }); } w("client/src");
